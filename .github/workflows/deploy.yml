# nano-demo Deployment Workflow
# Builds and deploys to demo server directly (no GHCR dependency)
#
# Triggers:
# - Push to main branch
# - Manual workflow dispatch
#
# Required secrets:
# - DEMO_SERVER_HOST: demo.kaosmaps.com
# - DEMO_SERVER_USER: root
# - SSH_PRIVATE_KEY: ed25519 private key
#
# Updated: 2025-12-19 12:30

name: Deploy Demo Showcase

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to Demo
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEMO_SERVER_HOST }}
          username: ${{ secrets.DEMO_SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e

            # Navigate to app directory
            mkdir -p /opt/kaosmaps/demo/showcase
            cd /opt/kaosmaps/demo/showcase

            # Clone or update repository
            if [ -d "repo" ]; then
              echo "Updating existing repository..."
              cd repo
              git fetch origin
              git reset --hard origin/main
            else
              echo "Cloning repository..."
              git clone https://github.com/kaosmaps/nano-demo.git repo
              cd repo
            fi

            # Create docker-compose.yml (build locally, not from GHCR)
            cat > docker-compose.yml << 'COMPOSE'
            # nano-demo Production (Built on server)
            services:
              web:
                build:
                  context: .
                  dockerfile: Dockerfile
                image: nano-demo:latest
                container_name: nano-demo
                networks:
                  - kaosmaps-demo
                restart: unless-stopped
                healthcheck:
                  test: ["CMD", "wget", "-q", "--spider", "http://localhost/health"]
                  interval: 30s
                  timeout: 5s
                  retries: 3

            networks:
              kaosmaps-demo:
                external: true
            COMPOSE

            # Build and deploy
            echo "Building image..."
            docker compose build --no-cache

            echo "Stopping old container..."
            docker compose down --remove-orphans || true

            echo "Starting container..."
            docker compose up -d

            # Verify deployment
            echo "Waiting for startup..."
            sleep 5

            # Check container is running
            docker ps | grep nano-demo

            echo "Deployment complete!"
            echo "Access at: https://demo.kaosmaps.com/"

      - name: Verify Deployment
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEMO_SERVER_HOST }}
          username: ${{ secrets.DEMO_SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            echo "=== Deployment Status ==="
            docker ps | grep nano-demo
            echo ""
            echo "=== Container Logs ==="
            docker logs nano-demo --tail 10 2>&1 || true
            echo ""
            echo "=== Cleanup Old Images ==="
            docker image prune -f
