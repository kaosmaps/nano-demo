# Kamal Deployment Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git (it's in .gitignore)

# Docker Registry (Docker Hub) credentials
KAMAL_REGISTRY_PASSWORD=your_dockerhub_token_or_password

# SSH Configuration (optional - Kamal can use your SSH agent)
# SSH_PRIVATE_KEY_PATH=~/.ssh/id_ed25519

# Server Configuration (set these in config/deploy.yml)
# HETZNER_SERVER_IP=your.server.ip.address
# DOCKERHUB_USERNAME=your_dockerhub_username
